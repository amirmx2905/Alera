apiVersion: v1
kind: Service
metadata:
  name: alera-web
  namespace: alera
  labels:
    app: alera-web
spec:
  type: NodePort
  selector:
    app: alera-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
